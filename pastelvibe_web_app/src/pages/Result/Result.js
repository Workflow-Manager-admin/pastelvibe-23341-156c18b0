import React from 'react';

// PUBLIC_INTERFACE
/**
 * Result page: Playlist + Moodboard (to be implemented).
 */
const Result = () => (
  <div className="container">
    <h2>Result</h2>
    <p>This is where your vibe's playlist and moodboard appear. (Page under construction)</p>
  </div>
);

export default Result;
