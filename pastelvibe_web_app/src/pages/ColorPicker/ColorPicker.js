import React from 'react';

// PUBLIC_INTERFACE
/**
 * Color Picker page: To be implemented (palette selection/color picker).
 */
const ColorPicker = () => (
  <div className="container">
    <h2>Color Picker</h2>
    <p>Select a pastel palette or create your own. (Page under construction)</p>
  </div>
);

export default ColorPicker;
